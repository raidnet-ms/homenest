---
esp8266:
  board: $board
  restore_from_flash: true
#  framework:
#    version: recommended
#  early_pin_init: false # Recommended value is false where switches are involved, as these will toggle when updating the firmware or when restarting the device. Defaults to true.


packages:
  device_base: !include ../device_base.yaml